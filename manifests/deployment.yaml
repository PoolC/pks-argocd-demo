---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-server
  namespace: pks-argocd-demo
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-server
  template:
    metadata:
      labels:
        app: demo-server
    spec:
      containers:
        - name: demo-server
          image: "ghcr.io/poolc/pks-argocd-demo:dbdedcb360abab5adc59d32b378f84c1dbcceb08"
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 128Mi
              cpu: 100m
